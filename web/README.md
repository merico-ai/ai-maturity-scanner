# AI Maturity Scanner Web

Next.js 14 App Router site for the AI Maturity Scanner landing pages.

```bash
npm install
npm run dev
```

The project is intentionally isolated from the root CLI package.
